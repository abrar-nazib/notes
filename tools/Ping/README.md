# Commands used so far for `ping`

- `ping domain.com` Will retrieve and show the ip address of the target.
- `-4` will ping for ipv4 only
- `-6` will ping for ipv6 only
- `-i <seconds>` sets intervals between each ping
