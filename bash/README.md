# Bash Script Rough Sheet

## If Statement

```bash
if[condition_to_check]
then
    dosomething
fi
```

## Substring

### Cut

```bash
cut -b int filename||text
    #selects the int number of the character and returns it
    #Not 0 based indexing: its 1 based
```
